import numpy as np
arr = np.array([3,2,0,1])

print(np.sort(arr))

engarr = np.array(['banana','cherry', 'apple'])
print(np.sort(engarr))

boolarr = np.array([True, False, True])
print(np.sort(boolarr))
